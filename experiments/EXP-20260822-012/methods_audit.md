# EXP-20260822-012 pre-execution methods audit

## Decision

**GO only after amendments, now frozen.** An independent outcome-blind critic found
the question defensible as a deterministic same-assay stress test because no
guide-omission outcome has been computed. It is not independent biological
confirmation.

## Amendments frozen

- EXP-012 is a new child of audited EXP-011; failed EXP-010 remains immutable.
- EXP-011 protocol, implementation, receipts, ledger, result, audit, and public
  commit are hash-bound.
- The passing/nonexcluded sequence rule, exact three-field joins, canonical
  booleans, `nPassingSequences` count equality, and strict guide fields are fixed.
- The 103/103 official-score and EXP-011-ledger gates run before the EXP-005 gap file
  is loaded or any omission loop is entered.
- The 34 reconstructed baseline percentiles and gaps must reproduce the frozen
  EXP-005 file within `1e-8`; the ten selected models come only from its stored
  booleans and are not re-selected.
- Even-guide median behavior is explicitly the sorted middle-two arithmetic mean.
- Exactly four global Avana-only and five global KY-only omissions run with complete
  full-denominator reranking, average midranks, and exact `gap >= 0.25` flags.
- The inherited primary gate is at least 8/10 locked models flagged in all nine
  omissions.
- No p-value, interval, bootstrap, guide ranking, subgroup test, combined
  perturbation, or guide-quality/causal interpretation is allowed.
- Selection at an extreme baseline threshold and the 4-to-3 versus 5-to-4 median
  asymmetry must be disclosed.

No omission, rank, percentile, gap, transition, or robustness outcome was inspected
or computed during this review.
