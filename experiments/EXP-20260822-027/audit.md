# EXP-20260822-027 audit

## Pre-endpoint gate

**GO.** The independent pre-endpoint audit verified the corrected outcome-free
census, design receipt, selection seal, and implementation manifest. It
recomputed 1,290 source/model rows from 1,292 eligible screens, Avana 975 and
KY 315 source/model units, ARID1A status counts 101/874 and 43/272, mixed-lineage
counts 19/11, the canonical roster SHA-256, and executable-engine planning
powers 0.8621 and 0.5760. The exact `EZH2 (2146)` header identity and frozen
endpoint-file hash were verified before score values were parsed. The corrected
planning receipt was committed before implementation; the selected pair and
all outcome-free context counts were unchanged by the correction.

## Post-execution checks

**GO.** The independent audit recomputed:

- the exact five-file canonical result set and 1,290-row context and endpoint
  ledgers;
- all four non-summary artifact SHA-256 values;
- the normalized summary digest and pre-endpoint receipt;
- source-specific design sensitivity, endpoint median collapse, delta,
  pair-count, permutation, bootstrap, and lineage-delta values;
- the runner's staged-file validation;
- the terminal `FEASIBILITY_ONLY`, `confirmatory_claim: false`, and
  `overall_pass: false` contract.

The recomputation matched Avana delta `-0.1925133689839572` and KY delta
`-0.08284714119019837`, including all pair counts, p-values, bootstrap
intervals, and lineage deltas. No source pooling, lineage exclusion, threshold
change, or endpoint-derived rescue was used.

The full repository suite passed: `140` tests.

## Claim decision

EXP027 is released only as a T1 descriptive feasibility result. It does not
support a general ARID1A/EZH2 dependency claim, a pharmacologic EZH2 inhibitor
claim, a causal synthetic-lethality claim, or any treatment, clinical, or
confirmatory claim.
