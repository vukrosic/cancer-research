# EXP036 audit

**GO.** The independent adequacy audit reproduced the exact frozen metadata
counts and confirmed that KY contains only 18 RB1-damaging models, below the
protocol minimum of 20. The endpoint header identity was checked without
parsing any SKP2 score row. No endpoint values, planning simulation, or
inferential statistic exists for this rejected candidate.

The original census, input hashes, threshold, and primary literature URL are
tracked in `candidate_census.json`. No post-outcome rescue or threshold change
was used.
